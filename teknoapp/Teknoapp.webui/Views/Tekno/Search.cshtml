@model ProductListViewModel
@section MessageBox{
    <div class="alert alert-primary text-center m-0">TeknoAPP'e Hoşgeldiniz!</div>
}
 <div class="container mt-1">
     <div class="row">     
        @foreach(var product in Model.Products)
        {
            <div class="col-md-4">
            @await Html.PartialAsync("_product",product)
        </div>
            }
            </div>          
        </div>
